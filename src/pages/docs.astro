---
import Layout from "../layouts/Layout.astro";
import BaselineDocs from "../components/ui/BaselineDocs.astro";

const lang = "en";
---

<Layout
	lang={lang}
	title="Theme Baseline"
	description="Hearth theme baseline report: semantic color matrix, readability budget, and change protocol."
>
	<BaselineDocs lang={lang} />
</Layout>
