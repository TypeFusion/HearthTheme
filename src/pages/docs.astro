---
import Layout from "../layouts/Layout.astro";
import BaselineDocs from "../components/ui/BaselineDocs.astro";

const lang = "en";
---

<Layout
	lang={lang}
	title="Color Language Baseline"
	description="HearthCode color language baseline: semantic matrix, readability budget, and change protocol across implementation surfaces."
>
	<BaselineDocs lang={lang} />
</Layout>
